# 🔴 Установка Redis для LinguaTrack

Redis используется как брокер сообщений для Celery (фоновые задачи и напоминания).

## ⚠️ Важно

**Для разработки Redis не обязателен!**

Если вы хотите быстро начать работу, установите в `.env`:

```env
USE_CELERY=False
```

В этом случае Celery будет использовать memory broker, и Redis не потребуется. Однако напоминания через Telegram работать не будут.

## 🪟 Установка Redis для Windows

### Вариант 1: WSL (рекомендуется для Windows)

Если у вас установлен WSL (Windows Subsystem for Linux):

```bash
# Запустите WSL
wsl

# Обновите пакеты
sudo apt update

# Установите Redis
sudo apt install redis-server

# Запустите Redis
redis-server
```

Redis будет доступен на `localhost:6379`.

### Вариант 2: Docker (рекомендуется)

Если у вас установлен Docker:

```bash
# Запустите Redis в контейнере
docker run -d -p 6379:6379 --name redis redis:latest

# Проверьте, что Redis запущен
docker ps
```

Redis будет доступен на `localhost:6379`.

**Остановка Redis:**
```bash
docker stop redis
```

**Запуск Redis:**
```bash
docker start redis
```

### Вариант 3: Memurai (нативный Redis для Windows)

1. Скачайте Memurai с официального сайта: https://www.memurai.com/
2. Установите Memurai (это Redis-совместимый сервер для Windows)
3. Memurai автоматически запустится как служба Windows
4. По умолчанию работает на `localhost:6379`

**Управление службой:**
```bash
# Запуск
net start Memurai

# Остановка
net stop Memurai
```

### Вариант 4: Chocolatey

Если у вас установлен Chocolatey:

```bash
choco install redis-64
```

## 🐧 Установка Redis для Linux

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install redis-server

# Запуск Redis
sudo systemctl start redis-server

# Автозапуск при загрузке
sudo systemctl enable redis-server

# Проверка статуса
sudo systemctl status redis-server
```

## 🍎 Установка Redis для macOS

```bash
# Используя Homebrew
brew install redis

# Запуск Redis
brew services start redis

# Или запуск вручную
redis-server
```

## ✅ Проверка установки

После установки проверьте, что Redis работает:

```bash
# Подключение к Redis CLI
redis-cli

# В Redis CLI выполните:
ping
# Должен вернуться: PONG

# Выход из CLI
exit
```

## 🔧 Настройка в проекте

После установки Redis обновите `.env`:

```env
USE_CELERY=True
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0
```

## 🚀 Запуск Celery с Redis

После настройки Redis запустите:

```bash
# Worker
celery -A linguatrack worker -l info

# Beat (для периодических задач)
celery -A linguatrack beat -l info
```

## 🐛 Решение проблем

### Ошибка: "Error 10061 connecting to localhost:6379"

**Причина:** Redis не запущен или недоступен.

**Решение:**
1. Убедитесь, что Redis запущен
2. Проверьте, что Redis слушает на порту 6379
3. Или установите `USE_CELERY=False` для использования memory broker

### Ошибка: "Connection refused"

**Причина:** Redis не запущен или блокируется файрволом.

**Решение:**
1. Запустите Redis
2. Проверьте настройки файрвола
3. Убедитесь, что Redis слушает на `127.0.0.1:6379`

### Redis работает, но Celery не подключается

**Решение:**
1. Проверьте `CELERY_BROKER_URL` в `.env`
2. Убедитесь, что `USE_CELERY=True`
3. Перезапустите Celery worker

## 📝 Примечания

- **Для разработки:** можно использовать `USE_CELERY=False` (memory broker)
- **Для продакшена:** обязательно используйте Redis
- **Порт по умолчанию:** 6379
- **Пароль:** по умолчанию Redis не требует пароль (для локальной разработки)

## 🔒 Безопасность (для продакшена)

Для продакшена рекомендуется:

1. Установить пароль для Redis
2. Настроить файрвол
3. Использовать SSL/TLS соединение
4. Ограничить доступ по IP

Подробнее: https://redis.io/topics/security
