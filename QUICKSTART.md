# 🚀 Быстрый старт LinguaTrack

Пошаговое руководство по запуску проекта.

## Шаг 1: Установка зависимостей

```bash
pip install -r requirements.txt
```

**Примечание:** Если возникают проблемы с компиляцией `aiohttp`, убедитесь, что используете Python 3.10+ и установлены все зависимости.

## Шаг 2: Настройка окружения

### 2.1. Создайте файл `.env`

Скопируйте `.env.example` в `.env`:

```bash
# Windows
copy .env.example .env

# Linux/Mac
cp .env.example .env
```

### 2.2. Заполните `.env` файл

Минимальная конфигурация для быстрого старта:

```env
# Django Settings
SECRET_KEY=django-insecure-change-this-in-production
DEBUG=True
ALLOWED_HOSTS=*

# Celery (можно отключить для разработки)
USE_CELERY=False

# Telegram Bot (получите у @BotFather)
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Text-to-Speech
TTS_LANGUAGE=en
```

**Важно:**
- Для быстрого старта можно установить `USE_CELERY=False` - Redis не потребуется
- Получите токен бота у [@BotFather](https://t.me/BotFather) в Telegram

## Шаг 3: Инициализация базы данных

```bash
# Применить миграции
python manage.py migrate

# Создать суперпользователя
python manage.py createsuperuser
```

## Шаг 4: Запуск компонентов

Откройте **4 отдельных терминала** для запуска всех компонентов:

### Терминал 1: Django сервер

```bash
python manage.py runserver
```

Веб-интерфейс будет доступен по адресу: http://127.0.0.1:8000

### Терминал 2: Telegram-бот

```bash
python manage.py run_bot
```

После запуска найдите вашего бота в Telegram и отправьте `/start` или нажмите любую кнопку.

### Терминал 3: Celery Worker (только если USE_CELERY=True)

```bash
celery -A linguatrack worker -l info
```

**Примечание:** Если `USE_CELERY=False`, этот шаг можно пропустить.

### Терминал 4: Celery Beat (только если USE_CELERY=True)

```bash
celery -A linguatrack beat -l info
```

**Примечание:** Если `USE_CELERY=False`, этот шаг можно пропустить. Напоминания работать не будут, но остальной функционал доступен.

## Шаг 5: Первое использование

### Веб-интерфейс

1. Откройте http://127.0.0.1:8000
2. Войдите с учетными данными суперпользователя
3. Создайте первую карточку: нажмите "Создать карточку"
4. Заполните форму:
   - **Слово**: `hello`
   - **Перевод**: `привет`
   - **Уровень**: `beginner`
5. Сохраните карточку

### Telegram-бот

1. Найдите вашего бота в Telegram
2. Отправьте `/start` или нажмите любую кнопку
3. Нажмите "➕ Добавить карточку"
4. Введите: `hello | привет` (или `hello / привет`)
5. Карточка будет создана и озвучена

## 📱 Основные функции бота

Бот использует удобные кнопки вместо команд:

| Кнопка | Описание |
|--------|----------|
| 📚 **Карточки на сегодня** | Показывает слова для повторения на сегодня |
| 🧪 **Тест** | Быстрый тест по карточкам |
| 📊 **Статистика** | Ваш прогресс изучения |
| 📝 **Мои карточки** | Список всех ваших карточек |
| ➕ **Добавить карточку** | Добавить новую карточку |
| 🔊 **Озвучить слово** | Произнести слово |

## 🔧 Решение проблем

### Ошибка: "ModuleNotFoundError: No module named 'celery'"

```bash
pip install -r requirements.txt
```

### Ошибка: "Cannot connect to redis://localhost:6379/0"

Установите `USE_CELERY=False` в `.env` или установите Redis (см. [REDIS_SETUP.md](REDIS_SETUP.md)).

### Бот не отвечает

1. Проверьте `TELEGRAM_BOT_TOKEN` в `.env`
2. Убедитесь, что бот запущен: `python manage.py run_bot`
3. Проверьте логи в `logs/linguatrack.log`

### Ошибка: "NoReverseMatch"

Убедитесь, что все миграции применены:

```bash
python manage.py migrate
```

## 📚 Следующие шаги

- Изучите [README.md](README.md) для полной документации
- Прочитайте [PROJECT_REVIEW.md](PROJECT_REVIEW.md) для анализа проекта
- Настройте Redis для полной функциональности (см. [REDIS_SETUP.md](REDIS_SETUP.md))

## ✅ Готово!

Теперь вы можете:
- ✅ Создавать карточки через веб-интерфейс
- ✅ Использовать Telegram-бота для изучения слов
- ✅ Проходить тесты и отслеживать прогресс
- ✅ Использовать озвучку слов

**Удачи в изучении языков! 🎓**
