# LinguaTrack

MVP-платформа для изучения иностранных слов с флеш-карточками, интервальным повторением (алгоритм SM-2), озвучкой и интеграцией с Telegram-ботом.

## 🚀 Основные возможности

- **Флеш-карточки**: создание, редактирование, удаление карточек со словами
- **Интервальное повторение**: алгоритм SM-2 для эффективного запоминания
- **Множественные режимы тестирования**: обычный тест, множественный выбор, сопоставление
- **Озвучка слов**: интеграция с Google TTS (gTTS) для произношения
- **Telegram-бот**: удобный интерфейс с кнопками для изучения слов "на ходу"
- **Статистика**: отслеживание прогресса, рекомендации по изучению
- **Импорт/Экспорт**: работа с CSV файлами

## 🛠 Технологии

- **Backend**: Django 4.2.7
- **Frontend**: Django Templates + Bootstrap 5
- **База данных**: SQLite
- **Telegram-бот**: aiogram 3.1+ (async)
- **Озвучка**: gTTS (Google Text-to-Speech)
- **Фоновая логика**: Celery 5.3.4 + Redis (или memory broker для разработки)
- **Алгоритм повторения**: SM-2 (SuperMemo 2)

## 📦 Установка

### 1. Клонируйте репозиторий или создайте проект

```bash
git clone <repository-url>
cd Lingua_Track
```

### 2. Создайте виртуальное окружение (рекомендуется)

```bash
python -m venv venv
# Windows
venv\Scripts\activate
# Linux/Mac
source venv/bin/activate
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

### 4. Настройте переменные окружения

Создайте файл `.env` на основе `.env.example`:

```bash
# Windows
copy .env.example .env
# Linux/Mac
cp .env.example .env
```

Заполните `.env` файл:

```env
# Django Settings
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=*

# Celery Configuration (опционально)
# Если Redis не установлен, оставьте USE_CELERY=False
USE_CELERY=False
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# Telegram Bot
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# Text-to-Speech Settings
TTS_LANGUAGE=en
```

**Важно:**
- Получите токен бота у [@BotFather](https://t.me/BotFather) в Telegram
- Для разработки можно использовать `USE_CELERY=False` (будет использован memory broker)
- Для продакшена установите `DEBUG=False` и укажите конкретные `ALLOWED_HOSTS`

### 5. Примените миграции

```bash
python manage.py migrate
```

### 6. Создайте суперпользователя

```bash
python manage.py createsuperuser
```

## 🏃 Запуск

### Веб-интерфейс

```bash
python manage.py runserver
```

Откройте браузер: http://127.0.0.1:8000

### Telegram-бот

```bash
python manage.py run_bot
```

Или напрямую:

```bash
python -m bot.bot
```

### Celery Worker (если USE_CELERY=True)

В отдельном терминале:

```bash
celery -A linguatrack worker -l info
```

### Celery Beat (для периодических напоминаний)

В отдельном терминале:

```bash
celery -A linguatrack beat -l info
```

**Примечание:** Если `USE_CELERY=False`, Celery будет использовать memory broker и Redis не требуется.

## 📱 Использование

### Веб-интерфейс

- **Карточки**: создание, просмотр, редактирование, удаление
- **Сегодня**: список карточек для повторения на сегодня
- **Тесты**: обычный тест, множественный выбор, сопоставление
- **Импорт/Экспорт**: работа с CSV файлами
- **Статистика**: прогресс изучения, рекомендации

### Telegram-бот

Бот использует удобные кнопки вместо команд:

- **📚 Карточки на сегодня** - список слов для повторения
- **🧪 Тест** - быстрый тест по карточкам
- **📊 Статистика** - ваш прогресс
- **📝 Мои карточки** - список всех карточек
- **➕ Добавить карточку** - добавление новой карточки (формат: `слово | перевод` или `слово / перевод`)
- **🔊 Озвучить слово** - произношение слова

**Примеры использования:**
- Добавление карточки: нажмите "➕ Добавить карточку", затем введите `hello | привет`
- Озвучка: нажмите "🔊 Озвучить слово", затем введите слово для озвучки

## 📁 Структура проекта

```
linguatrack/
├── manage.py
├── requirements.txt
├── .env.example
├── linguatrack/
│   ├── settings.py          # Настройки Django
│   ├── urls.py              # Главные URL
│   ├── celery.py            # Конфигурация Celery
│   └── __init__.py
├── cards/                   # Приложение для карточек
│   ├── models.py           # Модель Card
│   ├── views.py            # Представления (CRUD, тесты)
│   ├── urls.py             # URL маршруты
│   ├── forms.py            # Формы Django
│   ├── services.py         # Бизнес-логика
│   ├── tts.py              # Сервис озвучки
│   ├── signals.py          # Сигналы (автосоздание Schedule)
│   └── templates/
├── schedules/              # Приложение для расписания
│   ├── models.py          # Модель Schedule (SM-2)
│   └── services.py        # Алгоритм SM-2
├── stats/                 # Приложение для статистики
│   ├── models.py         # UserProfile, Stats
│   ├── views.py          # Представления статистики
│   ├── services.py       # Бизнес-логика статистики
│   └── templates/
├── bot/                   # Telegram-бот
│   ├── bot.py            # Основная логика бота
│   ├── tasks.py          # Celery задачи (напоминания)
│   └── management/
│       └── commands/
│           └── run_bot.py # Команда для запуска бота
└── templates/             # Шаблоны Django
```

## 🧮 Алгоритм SM-2

Проект использует алгоритм SuperMemo 2 для интервального повторения:

- **Качество ответа**: оценивается от 0 до 5
- **Интервал повторения**: рассчитывается автоматически на основе качества
- **Фактор легкости**: корректируется после каждого повторения
- **Автоматическое планирование**: следующее повторение назначается автоматически

## 🔧 Настройка для продакшена

1. Установите `DEBUG=False` в `.env`
2. Укажите конкретные `ALLOWED_HOSTS` (через запятую)
3. Сгенерируйте безопасный `SECRET_KEY`:
   ```python
   from django.core.management.utils import get_random_secret_key
   print(get_random_secret_key())
   ```
4. Настройте HTTPS (`SECURE_SSL_REDIRECT=True`)
5. Настройте логирование (уже настроено в `settings.py`)

## 📝 Логирование

Логи сохраняются в:
- `logs/linguatrack.log` - общие логи приложения
- Консоль - для разработки

Настроены отдельные логгеры для:
- `cards` - работа с карточками
- `bot` - Telegram-бот

## 🐛 Решение проблем

### Celery не подключается к Redis

Если Redis не установлен, установите `USE_CELERY=False` в `.env`. Celery будет использовать memory broker.

### Бот не отвечает

1. Проверьте `TELEGRAM_BOT_TOKEN` в `.env`
2. Убедитесь, что бот запущен: `python manage.py run_bot`
3. Проверьте логи в `logs/linguatrack.log`

### Ошибки миграций

```bash
python manage.py makemigrations
python manage.py migrate
```

## 📚 Дополнительная документация

- [QUICKSTART.md](QUICKSTART.md) - быстрый старт
- [REDIS_SETUP.md](REDIS_SETUP.md) - установка Redis
- [PROJECT_REVIEW.md](PROJECT_REVIEW.md) - анализ проекта

## 📄 Лицензия

MIT

## 👨‍💻 Разработка

Проект готов к использованию и дальнейшему развитию. Все основные функции реализованы и протестированы.

**Статус:** ✅ Готов к продакшену (после настройки переменных окружения)
